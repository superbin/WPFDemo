<Window x:Class="WpfDemos.ListBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDemos"
        mc:Ignorable="d"
        Title="ListBox" Height="450" Width="800">
    <Grid>
        <ListBox x:Name="lstBox" Width="200" Height="100" 
                 SelectionMode="Extended">
            <ListBoxItem Content="Item 1"/>
            <ListBoxItem Content="Item 2" IsSelected="True"/>
            <ListBoxItem Content="Item 3"/>
        </ListBox>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0 150 0 0">
            <Button x:Name="AddItem" Content="+" Width="20" Height="20" Click="AddItem_Click" Margin="10 0"/>
            <Button x:Name="DeleteItem" Content="-" Width="20" Height="20" Click="DeleteItem_Click" Margin="10 0"/>
        </StackPanel>
    </Grid>
</Window>
